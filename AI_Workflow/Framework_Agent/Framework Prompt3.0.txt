为当前项目建立一个轻量、可执行、面向 Agent 的开发约束体系。

生成以下文件：

AGENTS.md
docs/design/PROJECT_DESIGN.md
docs/design/SOURCEMAP.map
README.md

--------------------------------------------------

【事实来源】

设计文档：
@docs/design/PROJECT_DESIGN.md

模块索引：
@docs/design/SOURCEMAP.map

编码规范：
@lib-zos/docs/guide/guide_zos.md

docs/design/ 下设计文档为唯一事实来源（SSOT）。

SOURCEMAP.map 为模块定位索引。

实现必须与设计一致，不允许发明未定义结构。

--------------------------------------------------

【核心约束】

- 只描述当前结构（as-is）
- 不设计未来架构
- 不新增未存在的模块或分层
- 基于现有代码结构总结
- 不改变当前整体架构
- 不发明不存在的功能
- 保持目录结构与实现一致
- 保持现有工程风格
- 优先最小可运行结构
- 优先解决根因，而不是堆叠补丁

默认：

- 只读取当前任务相关文件
- 禁止全仓扫描
- 禁止无关文件读取
- 禁止无关重构
- 禁止主动优化未要求内容

完成当前任务后立即停止：

- 不输出教学内容
- 不输出规划内容
- 不输出分析过程
- 不重复需求

--------------------------------------------------

【代码规范】

代码注释使用中文。

注释重点说明：

- 为什么这样做
- 边界条件
- 潜在风险
- 设计原因

禁止：

- 无意义注释
- 重复代码逻辑注释
- 描述显而易见行为

必须遵守：

@lib-zos/docs/guide/guide_zos.md

中的编码风格规范。

--------------------------------------------------

【嵌入式特殊要求】

涉及以下内容时：

- DMA
- Cache
- 中断
- 时钟
- 引脚复用
- 功耗
- Flash
- BLE事件链
- RTOS任务调度

必须说明：

1. 关键硬件约束

2. 潜在风险与触发条件

3. 规避方案


未经验证的硬件行为：

必须标注：

“需验证”

禁止：

- 编造寄存器地址
- 编造芯片特性
- 编造芯片勘误信息
- 假设不存在的硬件行为

--------------------------------------------------

【Agent行为约束】

默认行为：

- 优先通过 @docs/design/SOURCEMAP.map 理解模块职责
- 优先通过 @docs/design/SOURCEMAP.map 定位代码位置
- 根据 PROJECT_DESIGN.md 理解整体结构
- 默认只修改当前任务相关代码
- 默认避免跨模块修改
- 默认避免架构调整

禁止：

- 发明不存在模块
- 新增无实际用途抽象层
- 修改无关代码
- 大范围重构
- 未经需求主动优化

--------------------------------------------------

【扩大分析范围条件】

仅当怀疑问题涉及：

- 跨模块调用
- 全局状态
- 数据流
- 状态机
- 初始化流程
- 中断-DMA-BLE事件链
- 架构级问题
- 设计与实现冲突

允许扩大分析范围。


扩大范围时必须：

- 说明扩大原因
- 优先最小范围探索
- 保持分析目标明确
- 避免无意义全局扫描

--------------------------------------------------

【问题解决原则】

必须：

- 优先寻找根因
- 不只修复表面现象
- 不堆叠临时补丁
- 保持已有代码风格

如果问题来自结构问题：

允许进行最小必要重构。

重构要求：

- 控制影响范围
- 保持接口稳定
- 保持工程风格一致
- 不引入复杂抽象

--------------------------------------------------

【文档同步规则】

任何结构或逻辑变化后：

必须同步更新：

PROJECT_DESIGN.md

SOURCEMAP.map


保证：

- 代码
- 设计文档
- 模块索引

保持一致。


代码与设计不一致时：

优先更新设计文档。

禁止：

代码长期偏离设计。

--------------------------------------------------

【AGENTS.md 要求】

AGENTS.md 用于约束 Agent 行为。

必须包含：

## Source of Truth

@docs/design/PROJECT_DESIGN.md

@docs/design/SOURCEMAP.map


## Coding Style

@lib-zos/docs/guide/guide_zos.md


必须明确：

- PROJECT_DESIGN.md 是核心设计事实来源
- SOURCEMAP.map 是模块索引
- Agent 必须优先通过 SOURCEMAP.map 定位代码
- Agent 必须遵守编码规范
- 不允许发明未定义结构
- 实现必须与设计一致
- 代码变化必须同步更新设计文档

--------------------------------------------------

【PROJECT_DESIGN.md 要求】

作为系统核心设计文档。

只描述当前结构（as-is）。

必须包含：

# Overview

项目用途与整体说明。


# Architecture

必须包含模块索引表：

| Module | Path | File | Responsibility | Entry |
|--------|------|------|----------------|-------|


要求：

- 描述当前真实模块
- 不新增不存在模块
- 不设计未来架构


# Data Model

描述当前数据结构：

- 核心数据对象
- 数据关系
- 数据流转方式


# Flow

描述当前主要流程：

- 初始化流程
- 核心业务流程
- 关键事件流程


禁止包含：

- TODO
- Roadmap
- 演进历史
- 版本信息
- 未来规划

--------------------------------------------------

【SOURCEMAP.map 要求】

使用表格格式：

| Module | Responsibility | Entry | Key Files | Path |
|--------|----------------|-------|-----------|------|


要求：

- 仅覆盖核心模块
- Responsibility 使用一句话描述模块职责
- Entry 为入口函数或入口文件
- Key Files 为关键文件
- Path 为代码路径
- 内容保持简洁


禁止：

- 编写设计说明
- 编写未来规划
- 添加无关模块

--------------------------------------------------

【README.md 要求】

面向开发者。

简要说明：

- 项目用途
- 如何使用
- Agent驱动开发方式
- AGENTS.md 的作用
- docs/design/ 的作用
- PROJECT_DESIGN.md 和 SOURCEMAP.map 的作用
- 编码规范来源：

@lib-zos/docs/guide/guide_zos.md

--------------------------------------------------

【输出要求】

- 分文件输出
- 不解释
- 不建议
- 不输出分析过程
- 不输出教学内容
- 保持与现有工程风格和目录结构一致